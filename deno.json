{
  "tasks": {
    "upload": "deno run --allow-all upload.ts",
    "test": "deno test --allow-all --no-check",
    "compile": "deno compile --allow-all --no-check --output .bin/updown upload.ts"
  },
  "imports": {
    "@std/assert": "jsr:@std/assert@1"
  },
  "exclude": ["CHANGELOG.md"],
  "fmt": {
    "singleQuote": true,
    "semiColons": false
  },
  "lint": {
    "rules": {
      "exclude": ["no-explicit-any"]
    }
  },
  "nodeModulesDir": "auto"
}
